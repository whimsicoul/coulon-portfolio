# Environment variables for coulon-portfolio
#
# This project has no required environment variables for local development.
# The Railway deployment injects PORT automatically at runtime.
#
# If you fork this and add backend features, document new variables here.

# PORT is set automatically by Railway in production — do not set it locally.
# PORT=3000
